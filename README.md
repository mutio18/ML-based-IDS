# ML-based-IDS
This project implements a Machine Learning-based IDS using XGBoost to classify network traffic as normal or attack. Trained on the UNSW-NB15 dataset and augmented with real network captures (ARP, DHCP, DNS), the system supports manual input, CSV file analysis, and Wireshark integration through a simple command-line interface.
